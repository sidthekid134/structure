node_modules/
dist/
*.js.map

# Environment files
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test output
coverage/
.nyc_output/

# Build output and caches
build/
.cache/
.turbo/
.parcel-cache/
*.tsbuildinfo

# OS / editor files
.DS_Store
.vscode/
.idea/

# pkg CLI binaries (local builds)
binaries/

# pkg-fetch cache (Node binaries pulled from yao-pkg/pkg-fetch releases)
.pkg-cache/

# Updater signing — never commit private keys
*.key
*.key.pub
release/*.json
release/*.sig
!release/latest.template.json

# Local secrets / developer-specific identifiers
.secrets

# pnpm artifacts (project uses npm)
pnpm-lock.yaml
pnpm-workspace.yaml

# Claude Code worktrees / session state
.claude/worktrees/
